# סיכום פרויקט Wedding RSVP — מה עשינו ולמה

מסמך זה נועד בשבילך, כדי שתוכל לחזור ולהיזכר בלי לאבד את החוט. תעדכן אותו ככל שנתקדם.

---

## מה הפרויקט בכלל

אתר RSVP לחתונות. שני חלקים עיקריים:
- **דף ניהול** (`/admin`) — יוצרים "אירוע" חדש (זוג, תאריך, אולם)
- **דף אורח** (`/c/<couple_id>`) — כל זוג מקבל דף משלו, אורחים ממלאים אישור הגעה שם

**המחסנית (Stack):**
- Flask (Python) — הבק-אנד, בקובץ `app.py`
- HTML templates — `templates/admin.html`, `templates/index.html`
- בגרסה **האמיתית** (עתידית): AWS DynamoDB לאחסון + AWS Cognito ל-login
- Docker — לאריזת האפליקציה כך שתרוץ באותה צורה בכל מקום

---

## שני קבצי `app.py` — ואיך לא להתבלבל ביניהם

| קובץ | מה יש בו | מתי משתמשים |
|---|---|---|
| `app.py` (הגרסה שאנחנו עובדים איתה עכשיו) | **בלי** AWS/Cognito. שומר נתונים ב-`dev_data.json` מקומי. Login מדומה — נכנסים ישר בלי סיסמה. | פיתוח מקומי, לימוד, בדיקות |
| `app_aws_original.py` | הגרסה **האמיתית**, עם `boto3`, DynamoDB, Cognito | כשנחבר AWS אמיתי בעתיד |

⚠️ **תמיד תוודא באיזה `app.py` אתה עובד** — הכי קל: `head -20 app.py`. אם רואים `import boto3` זה הגרסה האמיתית; אם רואים docstring "`app_dev.py — גרסת פיתוח מקומית`" זה הגרסה שלנו.

---

## מבנה התיקייה שלך

```
wedding-rsvp/
├── app.py                    ← הגרסה בלי AWS (מה שרץ עכשיו)
├── app_aws_original.py       ← הגרסה האמיתית עם AWS, לעתיד
├── requirements.txt
├── Dockerfile
├── dev_data.json             ← נוצר אוטומטית, פה "המסד נתונים" המדומה שלנו
├── templates/
│   ├── admin.html
│   └── index.html
└── static/                   ← ריקה כרגע, אבל חייבת להתקיים בשביל Docker build

wedding-rsvp-infra/            ← תיקייה נפרדת, לתשתית (Terraform)
├── providers.tf               ← הגדרת ה-provider של AWS + גרסת Terraform
├── variables.tf                ← משתנים (region, שמות טבלאות)
├── main.tf                     ← המשאבים בפועל (כרגע: 2 טבלאות DynamoDB)
├── .terraform/                 ← נוצר אוטומטית ע"י terraform init, לא לגעת/להעלות ל-git
├── .terraform.lock.hcl         ← "נועל" גרסת ה-provider, כן להעלות ל-git
└── terraform.tfstate           ← "הזיכרון" של Terraform, מה כבר נוצר בפועל — רגיש! לא ל-git
```

---

## מושגי קוד שעברנו (ב-`app.py`)

- **`import`** — מביא כלים מוכנים (ספריות) לתוך הקובץ
- **`f-string`** (`f"{x}-{y}"`) — דרך לשלב משתנים בתוך טקסט
- **`.strip()` / `.lower()`** — ניקוי טקסט (רווחים מיותרים / אותיות קטנות)
- **`re.sub(תבנית, החלפה, טקסט)`** — regex, מחפש-ומחליף לפי דפוס
- **`dict` (מילון)** — אוסף של `מפתח: ערך` (למשל `couple_id: פרטי הזוג`)
- **`list` (רשימה)** — אוסף מסודר של פריטים, עם סדר
- **`.get(key, default)`** — שולף ערך ממילון, בלי לקרוס אם המפתח לא קיים
- **List comprehension** (`[x for x in y if ...]`) — דרך קצרה לכתוב לולאה שבונה רשימה
- **Decorator** (`@login_required`, `@app.route(...)`) — "עוטף" פונקציה בהתנהגות נוספת, בלי לשכפל קוד
- **`@app.route("/c/<couple_id>")`** — נתיב דינמי; מה שכתוב אחרי `/c/` נתפס אוטומטית כפרמטר
- **GET מול POST** — GET = "תן לי מידע", POST = "הנה מידע, תעשה איתו משהו"
- **קודי סטטוס HTTP** — 200 (הצלחה), 400 (בקשה שגויה מהלקוח), 404 (לא נמצא), 500 (שגיאת שרת)
- **`io.StringIO` / `io.BytesIO`** — "קובץ מדומה" בזיכרון, בלי לשמור קובץ אמיתי בדיסק
- **`jsonify(...)`** — הופך מילון פייתון לתשובת JSON תקנית

### 🐛 באג ידוע שזיהינו (עדיין לא תוקן)
בפונקציה `create_couple`, השדה:
```python
"created_at": str(uuid.uuid4())
```
זה **UUID אקראי**, לא תאריך אמיתי! לכן המיון ב-`/admin` (מהחדש לישן) בעצם לא עובד נכון — הוא ממיין לפי משהו אקראי. **פתרון עתידי:** להחליף ל-
```python
from datetime import datetime, timezone
"created_at": datetime.now(timezone.utc).isoformat()
```
**עדיין לא תיקנו את זה בפועל — משימה פתוחה.**

---

## מושגי DevOps / Docker שעברנו

### פורטים
- פורט = "דלת" ממוספרת שדרכה תוכנית מקשיבה לבקשות רשת
- פורט 80 = ברירת המחדל של HTTP (זו הסיבה שלא צריך לכתוב `:80` בכתובת)
- פורטים מתחת ל-1024 דורשים `sudo` בלינוקס
- `0.0.0.0` = "תקשיב מכל כתובת" (גם ממחשבים אחרים ברשת), לעומת `127.0.0.1`/`localhost` = "רק מהמחשב הזה"

### Docker — למה בכלל
פותר את בעיית "עבד אצלי במחשב" — אורז את כל הסביבה (מערכת הפעלה מינימלית + Python + ספריות + קוד) לחבילה סגורה שרצה אותו דבר בכל מקום.

### מושגי יסוד
| מושג | הסבר |
|---|---|
| **Dockerfile** | "מתכון" בטקסט — הוראות בנייה |
| **Image** | התוצר הבנוי מהמתכון — קפוא, קריא-בלבד |
| **Container** | image שרץ בפועל |
| **Registry** | "מחסן" לאחסון images (Docker Hub, AWS ECR) |

זרימה: `Dockerfile --(docker build)--> Image --(docker run)--> Container רץ`

### VM מול Container
VM = מערכת הפעלה מלאה נפרדת (כבד, איטי לעלות). Container = חולק kernel עם המחשב המארח, רק מבודד ברמת תהליך (קליל, מהיר).

### Dockerfile שלנו, שורה-שורה

```dockerfile
FROM python:3.13-slim       # image בסיס, גרסת "slim" = קליל יותר
WORKDIR /app                 # תיקיית עבודה בתוך ה-image
COPY requirements.txt .      # מעתיקים קודם רק את זה (ר' Layer Caching למטה!)
RUN pip install --no-cache-dir -r requirements.txt   # מתקין ספריות, בזמן build
COPY app.py .                 # רק עכשיו מעתיקים את הקוד עצמו
COPY templates/ ./templates/
COPY static/    ./static/     # התיקייה חייבת להתקיים, גם ריקה!
EXPOSE 5000                   # תיעוד בלבד — לא באמת פותח גישה
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]  # מה שרץ כשמפעילים container
```

### Layer Caching (מטמון שכבות) — הכלל הזהב
Docker בונה image בשכבות. כל שורה = שכבה. אם שכבה לא השתנתה מהפעם הקודמת — Docker משתמש בגרסה השמורה (מהיר!). ברגע ששכבה **כן** משתנה, **כל השכבות אחריה** נבנות מחדש גם הן.

**לכן:** דברים שמשתנים לעיתים רחוקות (`requirements.txt`) → למעלה בקובץ. דברים שמשתנים הרבה (הקוד שלך) → למטה. כך `pip install` הכבד לא רץ מחדש בכל שינוי קוד קטן.

### `RUN` מול `CMD`
| | `RUN` | `CMD` |
|---|---|---|
| מתי רץ | בזמן `docker build` | בזמן `docker run` |
| כמה אפשר | הרבה (כל אחד = שכבה) | רק אחד אחרון נחשב |
| נשמר ב-image? | כן, לצמיתות | לא, רק "הוראה מה להריץ" |

### gunicorn — למה לא `python app.py`
- `app.run()` הרגיל של Flask הוא **dev server** — חד-תהליכי, מטפל בבקשה אחת בכל רגע, לא עמיד לקריסות
- **gunicorn** מריץ כמה workers במקביל, מטפל בהרבה בקשות בו-זמנית, ומפעיל worker חדש אוטומטית אם אחד קורס
- לכן ה-`CMD` בDockerfile משתמש ב-gunicorn, לא ב-`python app.py`
- `"app:app"` = "בקובץ app.py, יש משתנה בשם app" (`app = Flask(__name__)`)

### פקודות שהרצנו בפועל

```bash
# בניית ה-image
docker build -t wedding-rsvp .

# הרצת container ברקע, עם מיפוי פורטים
docker run -d -p 8080:5000 --name wedding-rsvp-container wedding-rsvp

# רשימת images קיימים
docker images
```

- `-t wedding-rsvp` — שם קריא ל-image
- `.` — "תסתכל בתיקייה הנוכחית" (build context)
- `-d` — detached, רץ ברקע
- `-p 8080:5000` — `פורט_על_המחשב:פורט_בתוך_הקונטיינר`. זה מה **שבאמת** פותח גישה (בניגוד ל-`EXPOSE` שהוא רק תיעוד)
- `--name` — שם קריא לקונטיינר

**תוצאה:** האתר נגיש עכשיו ב-`http://localhost:8080/admin` — רץ בתוך container, לא ישירות דרך `python app.py`.

---

## פקודות Docker שימושיות לזכור

```bash
docker ps                              # אילו קונטיינרים רצים עכשיו
docker logs wedding-rsvp-container     # לוגים של הקונטיינר
docker stop wedding-rsvp-container     # לעצור
docker start wedding-rsvp-container    # להפעיל שוב (בלי לבנות מחדש)
docker rm wedding-rsvp-container       # למחוק קונטיינר (חייב לעצור קודם)
docker build -t wedding-rsvp .         # לבנות מחדש אחרי שינוי קוד
```

---

## AWS CLI — הגדרה ראשונית

**מה זה:** כלי שורת פקודה לתקשר עם AWS ישירות מהטרמינל, בלי דפדפן.

**התקנה (Ubuntu/WSL2):** דרך החבילה הרשמית (ZIP), לא `apt install awscli` (גרסה ישנה).

**חיבור לחשבון:**
```bash
aws configure
```
מבקש 4 דברים: Access Key ID, Secret Access Key, region (בחרנו `us-east-1`), output format (`json`).

**וידוא שהחיבור עובד:**
```bash
aws sts get-caller-identity
```
מחזיר מי אתה כרגע (UserId, Account, Arn) — לא נוגע בשום משאב, רק שאילת מידע.

### IAM — מושג יסוד לזכור
- **Root user** = הרשאות בלתי מוגבלות על כל החשבון. **לא** משתמשים בו לעבודה שוטפת.
- **IAM user** = משתמש עם הרשאות מוגבלות, ממוקדות למה שבאמת צריך (עיקרון **Least Privilege**).
- **Access Key ID** = כמו שם משתמש (לא סודי במיוחד)
- **Secret Access Key** = כמו סיסמה (סודי לגמרי, לא ל-git, לא לשתף) — **מוצג פעם אחת בלבד** ביצירה

---

## Terraform — Infrastructure as Code

**הרעיון:** במקום ליצור משאבים ב-AWS ידנית (קונסול או פקודות `aws` בודדות), **מתארים** בקבצי `.tf` איך רוצים שהתשתית תיראה, ו-Terraform דואג ליצור/לעדכן/למחוק בפועל כדי להתאים לתיאור.

### מבנה הקבצים שבנינו (בתיקיית `wedding-rsvp-infra/`)

**`providers.tf`** — אילו providers צריך (כאן: AWS) ובאיזו גרסה, ובאיזה region לעבוד.
```hcl
terraform {
  required_version = ">= 1.5.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"        # כל 5.x, לא קופץ ל-6.0 אוטומטית
    }
  }
}

provider "aws" {
  region = var.aws_region
}
```
שים לב: **אין** access_key/secret_key בקובץ — Terraform משתמש אוטומטית במה שהוגדר ב-`aws configure`.

**`variables.tf`** — משתנים ניתנים להתאמה (region, שמות טבלאות), עם ערכי ברירת מחדל.

**`main.tf`** — המשאבים בפועל. אצלנו: שתי טבלאות DynamoDB.

### מושגי יסוד ב-Terraform

| מושג | הסבר |
|---|---|
| `resource "סוג" "שם_פנימי" {...}` | מתאר משאב אמיתי שייווצר. ה"שם הפנימי" הוא רק לשימוש בתוך קבצי Terraform, לא משפיע על AWS |
| `var.X` | גישה לערך של משתנה שהוגדר ב-`variables.tf` |
| `terraform.tfvars` | קובץ אופציונלי לתת ערכים בפועל למשתנים (לא השתמשנו — יש לנו defaults) |
| `main.tf` מול קבצים מפוצלים | קונבנציה בלבד — ל-Terraform לא אכפת איך קוראים לקבצים, הוא קורא את כולם ביחד |

### זרימת העבודה שהרצנו בפועל

```bash
terraform init    # מוריד את ה-provider של AWS, פעם אחת לכל תיקייה
terraform plan     # "תצוגה מקדימה" — מראה מה ישתנה, לא נוגע בכלום בפועל
terraform apply    # יוצר בפועל, דורש הקלדת "yes" לאישור
```

- `terraform init` יוצר תיקייה נסתרת `.terraform/` וקובץ `.terraform.lock.hcl` (נועל גרסת provider)
- `terraform apply` יוצר/מעדכן קובץ `terraform.tfstate` — "הזיכרון" של Terraform, מה קיים בפועל. **קובץ רגיש — לא מעלים ל-git!**

### פקודות וידוא שימושיות

```bash
terraform state list                    # אילו משאבים Terraform עוקב אחריהם
aws dynamodb list-tables --region us-east-1   # בדיקה ישירה מול AWS (לא תלוי ב-Terraform)
```

---

## הטבלאות שיצרנו בפועל (`main.tf`)

| טבלה | מי כותב אליה | Partition Key | Sort Key | מה נשמר |
|---|---|---|---|---|
| `RSVP_Couples` | דף האדמין (`create_couple`) | `couple_id` | — | שם חתן/כלה, אולם, תאריך, שעות |
| `RSVP_Responses` | דף האורח (`submit`) | `couple_id` | `phone` | שם אורח, טלפון, כמות, האם מגיע, מנה |

**למה `RSVP_Responses` צריך גם Sort Key:** הרבה אורחים שייכים לאותו `couple_id`. בלי Sort Key, `couple_id` לבד היה חייב להיות ייחודי — אורח שני היה דורס את הראשון. הצירוף `(couple_id, phone)` הוא מה שבאמת ייחודי.

⚠️ **שינינו מהתשתית הישנה שהחבר בנה:** בגרסה הקודמת, `RSVP_Responses` היה עם `hash_key = "phone"` בלבד — זה היה שובר אם לאותו טלפון יש רשומות אצל שני זוגות שונים (מוזמן לשתי חתונות).

**billing_mode = PAY_PER_REQUEST:** משלמים רק לפי שימוש בפועל, אין עלות קבועה — מתאים לפרויקט לימוד/קטן.

---

## DynamoDB מול RDS מול DB על שרת EC2 — למה בחרנו DynamoDB

שאלה טובה להבין, גם אם היא "לא בדיוק העבודה שלך" כ-DevOps (בחירת ה-DB היא בדרך כלל החלטה של מפתחים/ארכיטקטים) — אבל DevOps טוב מבין את ה-trade-offs כי הם משפיעים ישירות על איך התשתית נראית.

### שלוש האפשרויות שיכולנו לבחור

**1. DynamoDB (מה שבחרנו) — NoSQL מנוהל לגמרי**
- AWS מנהלת הכל: שרתים, גיבויים, קנה מידה (scaling), תיקון תקלות — "בלי שרת" (serverless) מבחינתך
- לא צריך להגדיר קיבולת מראש (עם `PAY_PER_REQUEST`) — גדל/מצטמצם אוטומטית לפי עומס
- מודל נתונים גמיש: לא צריך schema קבוע מראש לכל שדה (רק למפתחות)
- **מגבלה:** לא מתאים לשאילתות מורכבות (JOIN בין טבלאות, שאילתות אנליטיות מסובכות) — מתוכנן לגישה מהירה לפי מפתח ידוע

**2. RDS — SQL מנוהל (Postgres/MySQL וכו')**
- גם מנוהל ע"י AWS (גיבויים, patches אוטומטיים), אבל **כן** צריך לבחור "גודל מכונה" (instance type) מראש
- שאילתות SQL מלאות, JOINs, טרנזקציות מורכבות — עוצמתי יותר ללוגיקה עסקית מסובכת
- **עלות:** משלמים על המכונה **גם כשאין תנועה בכלל** (בניגוד ל-DynamoDB PAY_PER_REQUEST) — פחות משתלם לפרויקט קטן/לימודי עם תנועה לא צפויה

**3. DB על שרת EC2 עצמאי — "עושים הכל בעצמנו"**
- אתה מקים שרת (EC2), מתקין עליו PostgreSQL/MySQL/MongoDB בעצמך, מנהל גיבויים בעצמך, patches בעצמך, scaling בעצמך
- **שליטה מקסימלית**, אבל גם **אחריות מקסימלית** — אתה "הDBA" עכשיו
- הכי הרבה עבודת תחזוקה, הכי הרבה סיכון לטעויות (גיבוי שנשכח, patch אבטחה שלא הותקן)

### למה DynamoDB התאים לנו ספציפית

- מודל הנתונים שלנו **פשוט** — "תן לי את כל האורחים של הזוג הזה", "תן לי זוג לפי ID" — בדיוק סוג הגישה ש-DynamoDB מעולה בו (lookup לפי מפתח), לא צריך JOINs
- פרויקט **לימוד/קטן** — אין טעם לשלם על מכונת RDS שיושבת ברובה בחוסר-פעילות
- **אפס תחזוקת שרתים** — אין לנו (עדיין) את הידע/זמן לנהל DB עצמאי על EC2, וזה גם לא הערך הלימודי שאנחנו מחפשים כרגע
- Serverless מתאים לאופי "עולה ויורד" של אירועי RSVP (הרבה תנועה סביב תאריך החתונה, כמעט כלום בשאר הזמן)

**מתי כן היינו בוחרים RDS?** אם היינו צריכים שאילתות מורכבות בין טבלאות (למשל "כל האורחים שלא ענו, מכל הזוגות, ממוינים לפי תאריך אירוע ואולם") — DynamoDB היה נהיה מסורבל, RDS היה טבעי יותר.

---

## איפה אנחנו עומדים עכשיו

✅ אפליקציה רצה מקומית עם Python ישירות
✅ אפליקציה רצה בתוך Docker container
✅ AWS CLI מותקן ומחובר לחשבון (IAM user, לא root)
✅ Terraform מותקן, מריץ `init`/`plan`/`apply` בהצלחה
✅ שתי טבלאות DynamoDB אמיתיות קיימות בענן (`RSVP_Couples`, `RSVP_Responses`)
⬜ תיקון הבאג של `created_at`
⬜ **חיבור app.py בפועל לטבלאות DynamoDB האמיתיות** (עדיין כותב ל-dev_data.json)
⬜ חיבור ל-Cognito אמיתי
⬜ פריסה לענן (deployment — EC2/ECS/Fargate)
⬜ CI/CD
